# Lambda module

Deploys one Python Lambda with KMS encryption, JSON logs, active X-Ray tracing, and
optional VPC configuration. The caller supplies a built artifact; this module never
creates application code or packages.
