# Coding standards

Python services use SOLID design, small cohesive modules, repository boundaries for
persistence, structured logging, explicit error handling, Ruff, Black, mypy, pytest,
and pre-commit. The full executable tool configuration is in `pyproject.toml`.
