# Runbooks

Runbooks provide safe, repeatable operational procedures. Each future runbook must
state its trigger, prerequisites, steps, verification, rollback or escalation path,
and owner. Do not include credentials or sensitive values.

No runtime services exist in Milestone 0, so no operational runbook is required yet.
